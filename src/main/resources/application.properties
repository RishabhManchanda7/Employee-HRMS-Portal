# Server Configuration
server.port=8080

# DynamoDB Configuration
aws.dynamodb.endpoint=http://localhost:8003
aws.dynamodb.region=ap-south-1
aws.access.key=fake
aws.secret.key=fake

# DynamoDB Table Configuration
aws.dynamodb.table.name=EmployeeHRMS

# App behavior
# Attendance and sample data always seeded on startup for demo/testing