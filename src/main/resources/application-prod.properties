# Production Configuration for AWS EC2
server.port=8080

# AWS DynamoDB Configuration (no endpoint = uses AWS service)
aws.dynamodb.region=us-east-1

# DynamoDB Table Configuration
aws.dynamodb.table.name=EmployeeHRMS

# Logging
logging.level.com.example.hrms=INFO